import messages.script.Msg0372.MSGID;


import Base;
import event.Battle;
import pokemon.PokeParty;
import system.EventFlags;
import system.EventWorks;
import event.actors.Actor;
import event.actors.Player;
import event.dialogs.Message;
import event.dialogs.YesNoWin;
import pokescript.vm.Math;
import pokescript.vm.StackCtrl;
import pokescript.vm.VM;
import system.Game;
import system.Fade;
import system.Input;
import system.Runtime;
import system.Sound;
import system.System;
import system.text.WordSet;
import system.Field;
import event.Trainer;

public class MainEvents {
	public static void main_1() { // @ 0xa
		Actor.PauseAll();
		Sound.SEPlay(1351);
		Actor.FacePlayer();
		Message.Actor(1024, 0, 0, 0);
		Input.LastKeyWait();
		Message.CloseActor();
		Runtime.FinishSubEvents();
		Actor.UnpauseAll();
	}

	public static void main_2() { // @ 0x26
		int v16;

		Actor.PauseAll();
		Sound.SEPlay(1351);
		WordSet.LoadPlayerName(0);
		Message.System(1, 0);
		v16 = YesNoWin.Show();
		if (v16 != 0) {
			goto LABEL_007c;
		}
		Message.System(2, 0);
		Message.CloseSystem();
		Fade.FadeEx(3, 0, 16, 2);
		Fade.FadeExWait();
		PokeParty.RecoverAll();
		Sound.MEPlay(1300);
		Sound.MEWait();
		Fade.FadeEx(3, 16, 0, 2);
		Fade.FadeExWait();
		Message.System(3, 0);
		Input.LastKeyWait();

		LABEL_007c:
		Message.CloseSystem();
		Runtime.FinishSubEvents();
		Actor.UnpauseAll();
	}

    // Scientist
    public static void main_3(){
		int isVictory; 
        int yesNo; 

        Actor.PauseAll();
		Sound.SEPlay(1351);
		Actor.FacePlayer();

        if (!EventFlags.Get(1520 + 444)){
			Sound.BGMPlay(1122);
            Message.Actor(1024, 4, 0, 0);
            yesNo = YesNoWin.Show();
            if (yesNo == 1){
                Message.Actor(1024, 6, 0, 0);
                Message.CloseAll();
            }
            else {
                Message.Actor(1024, 5, 0, 0);	
                Message.CloseAll();

				goto SKIP_BATTLE;
                Battle.CallTrainerBattle(444, 0, 0);
                isVictory = Battle.IsTrainerVictory();
                if (isVictory != 1){
                    Battle.CallTrainerLose();
                }
                Battle.EndTrainerBattle();
				SKIP_BATTLE:
				Field.PlayMapBGM();
                Message.Actor(1024, 7, 0, 0);
                Message.CloseAll();
                EventFlags.Set(1520 + 444);
                Global2805(390, 1); // CHANGE THIS LATER
            }
        }
        else {
            Message.Actor(1024, 8, 0, 0);
            Message.CloseAll();
        }
		
		Runtime.FinishSubEvents();
		Actor.UnpauseAll();
    }

    static meta void Global2805(int a1, int a2) : VGlobalCall[2805];
    
}   